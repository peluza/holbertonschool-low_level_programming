#include "holberton.h"
#include <stdio.h>

/**
 *  - long of the str
 *
 * @s: the caracter
 *
 */

void puts_half(char *str)
{
	char a;
	int x, c, i, d;

	a = str[0];

	for (x = 0; a != '\0'; x++)
	{
		a = str[x];
		c = x;
		if (
		c = c / 2;
	}

		for (i = c; str[c] != '\0'; i++)
		{
			_putchar(str[c]);
		}
	
_putchar('\n');
}
