#include "holberton.h"
#include <stdio.h>

/**
 * _strcat - the funtion is cocatene
 *
 * @dest: punter one
 * @src: punter two
 * Return: dest
 */

char *_strcat(char *dest, char *src)

{
	int x, y;

	for (x = 0; dest[x] != '\0'; x++)
	{
	}
	y = 0;
	while ((dest[x++] = src[y++]) != '\0')
	{
	}

return (dest);
}
